spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
    url: jdbc:mysql://localhost/straymanager?characterEncoding=utf-8&useSSL=false&allowPublicKeyRetrieval=true
  jpa:
    show-sql: true
server:
  port: 8080